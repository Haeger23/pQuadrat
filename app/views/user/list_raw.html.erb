<ul class="media-list">
  <% users.each do |user| %>
      <li class="media"><%= view("user", "list_item", user.attributes.merge({projects: user.projects, imageUrl: user.image.url(:medium)})) %></li>
  <% end %>
</ul>